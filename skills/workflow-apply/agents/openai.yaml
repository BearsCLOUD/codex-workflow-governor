interface:
  display_name: "Apply workflow draft"
  short_description: "Apply one compiled workflow revision."
  default_prompt: "Apply the saved draft for an explicit Workflow Governor workflow ID."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "workflow-governor"
      description: "Workflow Governor reader surface"
    - type: "mcp"
      value: "workflow-governor-maintainer"
      description: "Workflow Governor maintainer surface"

