interface:
  display_name: "Run workflow"
  short_description: "Run one explicit governed workflow."
  default_prompt: "Run an explicit Workflow Governor workflow ID."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "workflow-governor"
      description: "Workflow Governor reader surface"
    - type: "mcp"
      value: "workflow-governor-maintainer"
      description: "Workflow Governor maintainer surface"

