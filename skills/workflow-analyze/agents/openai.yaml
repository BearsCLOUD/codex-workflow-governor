interface:
  display_name: "Analyze workflow"
  short_description: "Separate workflow errors from advice."
  default_prompt: "Analyze an explicit Workflow Governor workflow ID."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "workflow-governor"
      description: "Workflow Governor reader surface"

