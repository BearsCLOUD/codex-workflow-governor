interface:
  display_name: "Update workflow draft"
  short_description: "Draft one explicit workflow revision."
  default_prompt: "Update an explicit Workflow Governor workflow ID without applying it."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "workflow-governor"
      description: "Workflow Governor reader surface"
    - type: "mcp"
      value: "workflow-governor-maintainer"
      description: "Workflow Governor maintainer surface"

