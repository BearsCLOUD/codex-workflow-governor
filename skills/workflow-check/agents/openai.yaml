interface:
  display_name: "Check workflow"
  short_description: "Validate one workflow deterministically."
  default_prompt: "Check an explicit Workflow Governor workflow ID in this repository."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "workflow-governor"
      description: "Workflow Governor reader surface"

