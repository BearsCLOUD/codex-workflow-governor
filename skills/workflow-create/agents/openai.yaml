interface:
  display_name: "Create workflow draft"
  short_description: "Draft one explicit governed workflow."
  default_prompt: "Create a Workflow Governor draft for this repository and an explicit workflow ID."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "workflow-governor-maintainer"
      description: "Workflow Governor maintainer surface"

