interface:
  display_name: "Visualize workflow"
  short_description: "Inspect or render workflow diagrams."
  default_prompt: "Visualize an explicit Workflow Governor workflow ID."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "mcp"
      value: "workflow-governor"
      description: "Workflow Governor reader surface"
    - type: "mcp"
      value: "workflow-governor-maintainer"
      description: "Workflow Governor maintainer surface"

