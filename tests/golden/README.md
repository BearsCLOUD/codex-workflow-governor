# Contract snapshots

These snapshots pin the public launcher help, MCP `tools/list`, deterministic
workflow show/plan output, and the stable status/result shape.  The status and
result fixture uses a valid deterministic run ID; live paths, prompts, and
model output are not included.
